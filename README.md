# SoundStream Neural Audio Codec

Реализация нейросетевого аудио-кодека на основе статьи:
Zeghidour, N., Luebs, A., Omran, A., Skoglund, J., & Tagliasacchi, M. (2021). SoundStream: An End-to-End Neural Audio Codec. arXiv:2107.03312.

## Cсылки на датасеты

1. Для обучения использован датасет LibriSpeech, часть train-clean-100  
2. Для подсчета метрик использован датасет LibriSpeech, часть test-clean
Ссылка на полный датасет LibriSpeech с нужными подразделами:
[LibriSpeech на Kaggle](https://www.kaggle.com/datasets/a24998667/librispeech)

## Структура репозитория

data/dataset.py           — предобработка датасета LibriSpeech
model/encoder_decoder.py  — архитектура энкодера и декодера
model/rvq.py              — Residual Vector Quantizer
model/discriminator.py    — Multi-Scale Wave + STFT дискриминатор
model/losses.py           — losses, используемые в нашей архитектуре
train.py                  — скрипт обучения
inference.py              — загрузка аудио и его прогон через кодек
evaluate.py               — подсчёт метрик STOI и NISQA
demo.ipynb                — демо в Google Colab для удобной загрузки аудио по `url`-ссылке
и его прогон через кодек. с удобным дисплеем и комментариями по использованию

## Отличие реализации от статьи

Следование статье было не полным, поэтому ниже указаны некоторые тонкости реализации:

1. Мы изменили параметры входного аудио с 24Гц на 16 Гц, а следовательно и некоторые численные параметры архитектуры для согласованности с этим изменением, подробнее в комментариях к реализации модели. Наиболее заметное изменение мы сделали в strides сверток для энкодера и декодера, вместо [2, 4, 5, 8] используем [2, 4, 5, 5]. (Обусловлено заданием.)

2. Мы изменили функцию активации в обоих дискриминаторах на `LeakyReLU(0.2)`. (Обусловлено заданием.)

3. Мы не используем `FiLM` в энкодере и декодере. (Обусловлено заданием.)

4. В `RVQ`(`Residual Vector Quantizer`) мы не делаем `EMA` (`Exponential Moving Average`). Мы делаем обычный `back propogation` (В задании не просили реализовывать `RVQ` в точности как в статье, а просили написать свою реализацию.) Так же мы не делам `Enabling bitrate scalability with quantizer dropout`.(Обусловлено заданием.)

5. В целом `RVQ` не включает себя многие детали из статьи, например, мы не подчищаем не исопользуемые векторы. Или мы инициализиуем кодбук рандомно, а не с помощью каких-то усреднений первого батча. Можно сказать, что это просто самая базовая реализация. Более подробно смотрите в комментариях к `rvq.py`.

6. Обучение происходит на датасете LibriSpeech train-clean-100, а подсчет метрик на LibriSpeech test-clean. Полные ссылки для скачивания датасетов указаны выше. То есть обучение было не на чистых и зашмуленных данных, а только на чистых. (Обусловлено заданием.)

7. В статье явно не описаны некоторые детали, например, shortcut в `ResidualUnit2D`, который пришлось сделать, чтобы размерности согласовывлись. Кажется, что это классическая практика при работе с residual connection, так как мы должны складывать выход и вход, которые могут оказаться разного размера.

8. В декодере есть тонкость с размерами входов. Либо это статья подразумевает, что это на самом деле количество выходов (так как декодер в некотором смысле "зеркальное отображение" энкодера), либо я не доразобралась, почему это не работает так, как хотелось бы. Возможно, мною из вида было упущено еще какое-то преобразование, на основании которого вход должен был быть больше, чем написано в моей реализации.

## Клонирование репозитория

По умолчанию здесь и в частях ниже имеется в виду, что вы используете среду выполнения с возможностью подключения к GPU, если не сказано иного. За основу взята среда Google Colab.

```python
!git clone https://github.com/sval9ka07/DLHW4.git
%cd DLHW4
```

## Установка среды

Здесь есть два случая:

1. Если вы не собираетесь запускать метрики, то вас устроит установка всего в `requiments.txt` кроме, соответственно `torchmetrics`. В случае Google Colab ноутбука

```python
!pip install torch==2.2.1 torchaudio==2.2.1 numpy==1.26.4 pystoi comet-ml huggingface_hub
```

2. Если же вы хотите установить весь пакет `requiments.txt`, потому что хотите запустить все и сразу, то вас ждет следующий нюанс. Вообще, команда, представленная ниже, в Google Colab ноутбук должна работать...

```python
!pip install -r requirments.txt
```

НО если вы запускаете все в новом ноутбуке - вам придется сразу перезапустить среду, так как Colab, судя по всему, автоматически подгружает другую версию `numpy` и мы получаем конфликт при использовании `nisqa`. То есть, в целом, не проблема. 

То есть, если вы уже с предсустановленным куда либо `numpy` - переустановка, перезапуск среды и т. д.

Если вы хотите сделать все уже в существующем ноутбуке, то помочь вам тоже может, в целом, только перезапуск среды, с новыми импортами в самом начале, чтобы Colab сразу их увидел и не подтянул другую версию `numpy` автоматически.

## Воспроизведение обучения

```python
!python train.py \
  --data_dir /путь/до/train-clean-100 \
  --use_comet \
  --comet_apikey YOUR_COMET_APIKEY \
  --comet_project YOUR_PROJECT_NAME \
  --hf_repo YOUR_HUGGING_FACE_REPO_NAME \
  --hf_token YOUR_HUGGING_FACE_REPO_TOKEN \
  --save_dir NAME_OF_DIR_FOR_CHECKPOINTS_IN_HF_REPO \
  --resume /путь/до/нужного/чекпоинта
```

Разумеется, некоторые из этих аргументов опциональны.

1. Основные аргументы
--data_dir - если вы скачиваете нужный датасет train-clean-100 по ссылке выше, то в зависимости от того, как вы его сохраните, вам нужно указать путь до этого датасета. Этого достаточно для обучения.

2. Если вы хотите логировать метрики в свой аккаунт CometML
--use_comet - вы должны указать, что вы вообще используете CometML

--comet_apikey - cюда передается ваш API для CometML

--comet_project - назание вашего проекта в СometML, по умолчанию назовется "soundstream"

3. Если вы хотите:
    (i) логгировать в HuggingFace промежуточные checkpoints и финальную модель, то
    --hf_repo - указваете название своего репозитория в HF (желательно создать его специально для этого проекта)
    --hf_token - токен, сгенерированный вами в вашем репозитории HF
    (ii) дополнительно к пункту (i) продолжить прерванное обучение с помощью одного из уже сохранившихся checkopoint
    --resume - указываете путь до последнего checkpoint, который у вас сохранился
